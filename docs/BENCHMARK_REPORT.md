# Benchmarking & Evaluation Report

**Streaming Live RAG — Samsung PRISM GenAI Hackathon, Theme 04**

Generated by `make eval` at 2026-09-21 00:45. Every number below is measured from that run; 
nothing is hand-entered. Re-running the command overwrites this file.

## 1. Run configuration

| | |
|---|---|
| embedder | `BAAI/bge-small-en-v1.5` |
| reranker | `cross-encoder/ms-marco-MiniLM-L-6-v2` |
| verifier | `cross-encoder/nli-deberta-v3-xsmall + lytang/MiniCheck-RoBERTa-Large` |
| llm | `gpt-5.4-mini` |
| decomposer | `gpt-4.1` |
| controller | `rule` |
| branches | `bm25,dense` |
| quota_per_intent | `3` |
| LLM configured | yes |
| replay speed | 1.0x |
| controller | `rule` |
| branches | `bm25,dense` |
| quota_per_intent | `3` |
| top_k | `12` |
| tau_stab | `0.95` |
| stab_run | `2` |
| merge_cos | `0.95` |
| support_min | `0.5` |
| llm | `auto` |
| reranker | `cross-encoder/ms-marco-MiniLM-L-6-v2` |

- **enterprise** corpus: 11 documents, 39 chunks
- **asqa** corpus: 8366 documents, 15679 chunks

Total wall clock: 0s.

## 2. Acceptance gates

### enterprise corpus

| Gate | Criterion | Measured | Threshold | Result |
|---|---|---|---|---|
| G1 | Reproducibility | 100 | one command builds the index and runs the suite with no manual step | **PASS** |
| G2 | Early retrieval | 88.9% | >= 80% of eligible turns, with a low false-trigger rate | **PASS** |
| G3 | Multi-intent identification | 75% | >= 70% of compound utterances isolate >= 2 distinct sub-intents | **PASS** |
| G4 | Factual grounding | 93.3% | >= 85% citation support and zero fabricated citations | **PASS** |
| G5 | Session refinement | 100% | every late-detail turn: version 2, parent 1, every parent claim kept or rewritten, no full-corpus search | **PASS** |
| G6 | Telemetry and observability | 100% | 100% of turns emit a complete trace record | **PASS** |

- G2 detail: 16/18 turns that should retrieve began retrieving before the utterance ended (the end-of-speech signal); median lead **264 ms**, max 7841 ms. Stricter: 22.2% began before the last word was spoken; the rest fired on the final words, in the pause before the end signal. False-trigger rate on the 4 turns that should not retrieve: 0%.
- G3 detail: 3/4 compound utterances isolated two or more sub-intents; full coverage of every gold intent 25%; over-fragmentation 12.5%; single-intent utterances kept single 75%.
  - by fixture family: {'compound': 75.0}
- G4 detail: support is pooled over every factual sentence the model wrote: 56 of 60 verified (a withheld sentence counts against it even though it never reached the user); per-turn mean 91.2%. 56 claims shipped across 18 answered turns; **0 fabricated citations shipped**, 0 blocked before shipping; recall@k (gold documents) 87.9% over 11 labelled turns; 7 turns flagged uncertainty.
- G4 independent audit: a second NLI model the engine never uses (`cross-encoder/nli-deberta-v3-base`) re-read the 56 shipped claims against the chunks they cite and agreed with 98.2% of them.
  - `compound_01` (support 0.004): Riverside Hall's in-house menu has Jain options .
- G5 `late_detail_01`: v2 from v1, 4 claims preserved, 0 rewritten, full-corpus search `False`, 5 chunks carried from session state.
- G5 `late_detail_02`: v2 from v1, 0 claims preserved, 0 rewritten, full-corpus search `False`, 7 chunks carried from session state (the parent answer verified no claims, so none could be kept).
- G5 `late_detail_03`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 5 chunks carried from session state.

### asqa corpus

| Gate | Criterion | Measured | Threshold | Result |
|---|---|---|---|---|
| G1 | Reproducibility | 100 | one command builds the index and runs the suite with no manual step | **PASS** |
| G2 | Early retrieval | 87.6% | >= 80% of eligible turns, with a low false-trigger rate | **PASS** |
| G3 | Multi-intent identification | 75% | >= 70% of compound utterances isolate >= 2 distinct sub-intents | **PASS** |
| G4 | Factual grounding | 86.9% | >= 85% citation support and zero fabricated citations | **PASS** |
| G5 | Session refinement | 100% | every late-detail turn: version 2, parent 1, every parent claim kept or rewritten, no full-corpus search | **PASS** |
| G6 | Telemetry and observability | 100% | 100% of turns emit a complete trace record | **PASS** |

- G2 detail: 92/105 turns that should retrieve began retrieving before the utterance ended (the end-of-speech signal); median lead **262 ms**, max 6340 ms. Stricter: 10.5% began before the last word was spoken; the rest fired on the final words, in the pause before the end signal. False-trigger rate on the 15 turns that should not retrieve: 0%.
- G3 detail: 30/40 compound utterances isolated two or more sub-intents; full coverage of every gold intent 22.5%; over-fragmentation 5%; single-intent utterances kept single 95%.
  - by fixture family: {'compound': 75.0}
- G4 detail: support is pooled over every factual sentence the model wrote: 146 of 168 verified (a withheld sentence counts against it even though it never reached the user); per-turn mean 88%. 146 claims shipped across 98 answered turns; **0 fabricated citations shipped**, 0 blocked before shipping; recall@k (gold passages) 85.5% over 74 labelled turns; 62 turns flagged uncertainty.
- G4 independent audit: a second NLI model the engine never uses (`cross-encoder/nli-deberta-v3-base`) re-read the 146 shipped claims against the chunks they cite and agreed with 93.2% of them.
  - `asqa_compound_08` (support 0.493): The document says that in 1600 the English scientist William Gilbert wrote De Magnete and made a careful study of electricity and magnetism .
  - `asqa_compound_25` (support 0.009): The Telcan was demonstrated on June 24, 1963, and the document says it was the first home video recorder .
  - `asqa_compound_26` (support 0.009): The retrieved documents also say this is described in Genesis 32:22–32 and referenced in Hosea 12:3–5, with the figure referred to as "man" and "God" in Genesis and as an "angel" in Hosea .
  - `asqa_compound_38` (support 0.004): Kobe Bryant's 2001 NBA Finals opponent was the Philadelphia 76ers .
  - `asqa_late_detail_02` (support 0.0): The plane crash is in the season finale of the eighth season of Grey's Anatomy .
- G5 `asqa_late_detail_01`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_02`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 6 chunks carried from session state.
- G5 `asqa_late_detail_03`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 4 chunks carried from session state.
- G5 `asqa_late_detail_04`: v2 from v1, 2 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_05`: v2 from v1, 2 claims preserved, 0 rewritten, full-corpus search `False`, 4 chunks carried from session state.
- G5 `asqa_late_detail_06`: v2 from v1, 2 claims preserved, 0 rewritten, full-corpus search `False`, 5 chunks carried from session state.
- G5 `asqa_late_detail_07`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 4 chunks carried from session state.
- G5 `asqa_late_detail_08`: v2 from v1, 1 claims preserved, 1 rewritten, full-corpus search `False`, 4 chunks carried from session state.
- G5 `asqa_late_detail_09`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 6 chunks carried from session state.
- G5 `asqa_late_detail_10`: v2 from v1, 0 claims preserved, 0 rewritten, full-corpus search `False`, 10 chunks carried from session state (the parent answer verified no claims, so none could be kept).
- G5 `asqa_late_detail_11`: v2 from v1, 0 claims preserved, 2 rewritten, full-corpus search `False`, 12 chunks carried from session state.
- G5 `asqa_late_detail_12`: v2 from v1, 0 claims preserved, 0 rewritten, full-corpus search `False`, 9 chunks carried from session state (the parent answer verified no claims, so none could be kept).
- G5 `asqa_late_detail_13`: v2 from v1, 0 claims preserved, 1 rewritten, full-corpus search `False`, 5 chunks carried from session state.
- G5 `asqa_late_detail_14`: v2 from v1, 1 claims preserved, 1 rewritten, full-corpus search `False`, 5 chunks carried from session state.
- G5 `asqa_late_detail_15`: v2 from v1, 2 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.

## 3. Latency and cost

| Corpus | Turns | Retrieval lead (median) | TTFT (median) | TTFT p90 | Turn complete (median) | Cost/turn | Tokens/turn |
|---|---|---|---|---|---|---|---|
| enterprise | 22 | 264 ms | 3627 ms | 8704 ms | 3939 ms | $0.00365377 | 2568.18 |
| asqa | 120 | 262 ms | 5218 ms | 40080 ms | 5384 ms | $0.00480356 | 3291.65 |

Retrieval lead is measured from the trace: `utterance_end_ms - first_retrieval_ms`. 
TTFT is measured from the end of the utterance to the first *validated* token, because the engine 
releases a sentence only after its citations resolve and its support clears the bar.

## 4. Against a baseline pipeline

The baseline is a conventional RAG turn on the same fixtures, with the same retriever, reranker, synthesiser and verifier: nothing happens while the user speaks, then the whole utterance is one search (`SLR_CONTROLLER=batch`, `SLR_DECOMPOSE=off`). It has no notion of a presentation-only turn or of a late detail, so it searches on both. The difference between the rows is exactly what streaming, decomposition and session refinement add.

### enterprise corpus

| Pipeline | Retrieval before speech ends | Searches on reformat turns | Multi-intent (G3) | Late detail refined, not restarted (G5) | Citation support | Recall@k | Fabricated | TTFT after speech | Answer complete after speech | Cost/turn |
|---|---|---|---|---|---|---|---|---|---|---|
| streaming live RAG | 88.9% | 0% | 75% | 100% | 93.3% | 87.9% (documents) | 0 | 3627 ms | 3939 ms | $0.003654 |
| baseline (batch RAG) | 0% | 100% | 0% | 0% | 95.2% | 84.8% (documents) | 0 | 2314 ms | 2627 ms | $0.001135 |

### asqa corpus

| Pipeline | Retrieval before speech ends | Searches on reformat turns | Multi-intent (G3) | Late detail refined, not restarted (G5) | Citation support | Recall@k | Fabricated | TTFT after speech | Answer complete after speech | Cost/turn |
|---|---|---|---|---|---|---|---|---|---|---|
| streaming live RAG | 87.6% | 0% | 75% | 100% | 86.9% | 85.5% (passages) | 0 | 5218 ms | 5384 ms | $0.004804 |
| baseline (batch RAG) | 0% | 100% | 0% | 0% | 96.1% | 65.3% (passages) | 0 | 2168 ms | 2228 ms | $0.001017 |

## 5. Ablations

### retrieval branches

_Does the lexical branch earn its place, or would dense-only do?_

| Arm | Early retrieval | Multi-intent | Citation support | Recall@k | Intents with evidence | TTFT | Cost/turn |
|---|---|---|---|---|---|---|---|
| hybrid (bm25+dense) | 79.5% | 70.5% | 76.2% | 79% (passages) | 100% (0 starved) | 5361 ms | $0.005116 |
| dense only | 79.5% | 72.7% | 79.5% | 78.5% (passages) | 100% (0 starved) | 5847 ms | $0.005036 |


### per-intent quota

_What happens to the quiet sub-intent when the context window is filled by score alone?_

| Arm | Early retrieval | Multi-intent | Citation support | Recall@k | Intents with evidence | TTFT | Cost/turn |
|---|---|---|---|---|---|---|---|
| quota = 3 per intent | 79.5% | 72.7% | 78.6% | 78.6% (passages) | 100% (0 starved) | 4194 ms | $0.005089 |
| quota = 0 (global top-k) | 79.5% | 72.7% | 81.4% | 79.8% (passages) | 100% (0 starved) | 4220 ms | $0.005178 |


### cross-encoder reranker

_The fused order is already relevance-sorted; what does a cross-encoder pass add?_

| Arm | Early retrieval | Multi-intent | Citation support | Recall@k | Intents with evidence | TTFT | Cost/turn |
|---|---|---|---|---|---|---|---|
| cross-encoder rerank + margin cut | 79.5% | 70.5% | 75.9% | 79.8% (passages) | 100% (0 starved) | 4970 ms | $0.005482 |
| fused order, no rerank | 79.5% | 70.5% | 87.8% | 76.4% (passages) | 100% (0 starved) | 5243 ms | $0.005316 |


### retrieval controller

_Is a model call per transcript chunk worth what it costs?_

| Arm | Early retrieval | Multi-intent | Citation support | Recall@k | Intents with evidence | TTFT | Cost/turn |
|---|---|---|---|---|---|---|---|
| rule controller | 79.5% | 68.2% | 82.6% | 78.2% (passages) | 100% (0 starved) | 3432 ms | $0.005137 |
| model controller | 0% | 77.3% | 82.4% | 77.6% (passages) | 100% (0 starved) | 4396 ms | $0.005169 |


## 6. Analysed edge-case failures

92 failure instances across 5 categories, all taken from the run above.

### 1. under decomposition (27 instances)

**Example** — `compound_01` (enterprise corpus)

> I need to plan a customer workshop in Pune for 30 people, and I need the cancellation policy and the catering options.

**What happened.** 3 gold sub-intents, 2 sub-queries produced.

**Why.** Fewer sub-queries than gold sub-intents. Two different causes hide behind one symptom. On a request that bundles several asks it is a missed split: the clause splitter found no boundary, usually because the speaker used no punctuation and no conjunction that opens a new request. On an ASQA question it is not a parsing failure at all. 'Who has the highest goals in world football' has no second clause to find, and enumerating its readings (men's, women's, all-time) needs knowledge of the domain, which the deterministic splitter lacks and a language model has. This is where the model-backed decomposer earns its cost.

**Trace evidence.** `{'gold': ['Pune workshop venue with capacity for 30 people', 'venue cancellation and refund policy', 'catering options for a workshop in Pune'], 'predicted': ['customer workshop Pune venue current cancellation policy', 'customer workshop Pune catering options for 30 people'], 'merged_by_guard': 0, 'method': 'llm'}`

Other instances: asqa_compound_01, asqa_compound_04, asqa_compound_05, asqa_compound_06, asqa_compound_07, asqa_compound_11, asqa_compound_12, asqa_compound_14

### 2. starved sub intent (27 instances)

**Example** — `late_detail_01` (enterprise corpus)

> Summarize the travel reimbursement rule for an employee trip.

**What happened.** A sub-intent reached the answer with no evidence of its own.

**Why.** A sub-intent reached synthesis with no evidence of its own. With the per-intent quota on this is usually a retrieval miss rather than a fusion one: the sub-query returned nothing above the margin cut.

**Trace evidence.** `{'sub_queries': ['employee travel reimbursement policy summary current'], 'quota_applied': False}`

Other instances: asqa_compound_14, asqa_compound_19, asqa_late_detail_02, asqa_late_detail_03, asqa_late_detail_09, asqa_single_01, asqa_single_02, asqa_single_03

### 3. claim withheld (19 instances)

**Example** — `compound_03` (enterprise corpus)

> Can you tell me how far in advance a venue has to be booked, and also which external caterers are approved for Baner Tech Hub?

**What happened.** 1 generated claim(s) failed verification and were withheld.

**Why.** The grounding verifier could not support a generated sentence from its cited chunk, so the sentence was withheld and reported as uncertainty instead of shipped. That is the intended behaviour, but each case is worth reading: a conservative verifier costs recall of true statements.

**Trace evidence.** `{'uncertainty': ['Not verified in the cited documents: The retrieved documents name GreenLeaf Kitchens as an approved external caterer for Pune, but they do not provide a full current list of approved external caterers for Baner Tech Hub', 'The full current list of approved external caterers for Baner Tech Hub could not be verified from the retrieved documents.'], 'support_rate': 0.6667, 'verifier': 'cross-encoder/nli-deberta-v3-xsmall + lytang/MiniCheck-RoBERTa-Large'}`

Other instances: asqa_compound_11, asqa_compound_12, asqa_compound_13, asqa_compound_14, asqa_compound_24, asqa_compound_39, asqa_late_detail_01, asqa_late_detail_02

### 4. late trigger (15 instances)

**Example** — `compound_04` (enterprise corpus)

> how much is the hotel limit for business trips in New York and what is the catering budget per attendee for a customer workshop

**What happened.** No provisional search fired before the speaker finished; retrieval began at the utterance end.

**Why.** The controller waits for content sufficiency and either semantic stability or a closed clause. A short utterance can end before any of those fire, so retrieval starts at the utterance end. This is the deliberate side of the trade: lowering the bar buys earlier retrieval and pays for it in searches on incomplete thoughts.

**Trace evidence.** `{'first_retrieval_ms': 14428, 'utterance_end_ms': 12108, 'decisions': [('wait', 'insufficient_content'), ('wait', 'insufficient_content'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('retrieve', 'utterance_complete')]}`

Other instances: asqa_compound_02, asqa_compound_06, asqa_compound_07, asqa_compound_11, asqa_compound_13, asqa_compound_21, asqa_compound_23, asqa_compound_26

### 5. over fragmentation (4 instances)

**Example** — `single_03` (enterprise corpus)

> What is the per diem rate and hotel limit when travelling to London for work?

**What happened.** 2 sub-queries for 1 gold sub-intents.

**Why.** More sub-queries than gold sub-intents — the failure the merge guard and the cap exist to bound. Extra near-duplicate queries dilute the reranker's budget and buy nothing.

**Trace evidence.** `{'gold': ['What is the per diem rate and hotel limit when travelling to London for work?'], 'predicted': ['current per diem rate business travel London', 'current hotel nightly rate limit business travel London']}`

Other instances: asqa_compound_03, asqa_compound_08, asqa_single_14

## 7. Reproducing this

```bash
docker compose up --build      # or: make up
make eval                      # rebuilds any missing index, runs every fixture, rewrites this file
```

Fixtures live in `evals/fixtures/`, corpora in `evals/corpora/` and `data/corpus/` (ASQA, built by `make dataset`). 
`evals/results/latest.json` holds the full payload this report was rendered from, 
including every per-turn trace field the gates read.

