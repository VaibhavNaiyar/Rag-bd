# Benchmarking & Evaluation Report

**Streaming Live RAG — Samsung PRISM GenAI Hackathon, Theme 04**

Generated by `make eval` at 2026-09-19 21:57. Every number below is measured from that run; 
nothing is hand-entered. Re-running the command overwrites this file.

## 1. Run configuration

| | |
|---|---|
| embedder | `BAAI/bge-small-en-v1.5` |
| reranker | `cross-encoder/ms-marco-MiniLM-L-6-v2` |
| verifier | `cross-encoder/nli-deberta-v3-xsmall` |
| llm | `gpt-4o-mini` |
| decomposer | `gpt-4.1` |
| controller | `rule` |
| branches | `bm25,dense` |
| quota_per_intent | `2` |
| LLM configured | yes |
| replay speed | 1.0x |
| controller | `rule` |
| branches | `bm25,dense` |
| quota_per_intent | `2` |
| top_k | `8` |
| tau_stab | `0.95` |
| stab_run | `2` |
| merge_cos | `0.95` |
| support_min | `0.5` |
| llm | `auto` |
| reranker | `cross-encoder/ms-marco-MiniLM-L-6-v2` |

- **enterprise** corpus: 11 documents, 39 chunks
- **asqa** corpus: 8366 documents, 15679 chunks

Total wall clock: 4905.7s.

## 2. Acceptance gates

### enterprise corpus

| Gate | Criterion | Measured | Threshold | Result |
|---|---|---|---|---|
| G1 | Reproducibility | 100 | one command builds the index and runs the suite with no manual step | **PASS** |
| G2 | Early retrieval | 88.9% | >= 80% of eligible turns, with a low false-trigger rate | **PASS** |
| G3 | Multi-intent identification | 75% | >= 70% of compound utterances isolate >= 2 distinct sub-intents | **PASS** |
| G4 | Factual grounding | 89.5% | >= 85% citation support and zero fabricated citations | **PASS** |
| G5 | Session refinement | 100% | every late-detail turn: version 2, parent 1, claims preserved, no full-corpus search | **PASS** |
| G6 | Telemetry and observability | 100% | 100% of turns emit a complete trace record | **PASS** |

- G2 detail: 16/18 turns that should retrieve began retrieving before the utterance ended (the end-of-speech signal); median lead **259 ms**, max 6499 ms. Stricter: 22.2% began before the last word was spoken; the rest fired on the final words, in the pause before the end signal. False-trigger rate on the 4 turns that should not retrieve: 0%.
- G3 detail: 3/4 compound utterances isolated two or more sub-intents; full coverage of every gold intent 50%; over-fragmentation 12.5%; single-intent utterances kept single 75%.
  - by fixture family: {'compound': 75.0}
- G4 detail: support is pooled over every factual sentence the model wrote: 85 of 95 verified (a withheld sentence counts against it even though it never reached the user); per-turn mean 88%. 85 claims shipped across 20 answered turns; **0 fabricated citations shipped**, 0 blocked before shipping; recall@k (gold documents) 87.9% over 11 labelled turns; 19 turns flagged uncertainty.
- G4 independent audit: a second NLI model the engine never uses (`cross-encoder/nli-deberta-v3-base`) re-read the 85 shipped claims against the chunks they cite and agreed with 100% of them.
- G5 `late_detail_01`: v2 from v1, 7 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `late_detail_02`: v2 from v1, 5 claims preserved, 1 rewritten, full-corpus search `False`, 4 chunks carried from session state.
- G5 `late_detail_03`: v2 from v1, 1 claims preserved, 1 rewritten, full-corpus search `False`, 3 chunks carried from session state.

### asqa corpus

| Gate | Criterion | Measured | Threshold | Result |
|---|---|---|---|---|
| G1 | Reproducibility | 100 | one command builds the index and runs the suite with no manual step | **PASS** |
| G2 | Early retrieval | 87.6% | >= 80% of eligible turns, with a low false-trigger rate | **PASS** |
| G3 | Multi-intent identification | 60% | >= 70% of compound utterances isolate >= 2 distinct sub-intents | **FAIL** |
| G4 | Factual grounding | 78.8% | >= 85% citation support and zero fabricated citations | **FAIL** |
| G5 | Session refinement | 100% | every late-detail turn: version 2, parent 1, claims preserved, no full-corpus search | **PASS** |
| G6 | Telemetry and observability | 100% | 100% of turns emit a complete trace record | **PASS** |

- G2 detail: 92/105 turns that should retrieve began retrieving before the utterance ended (the end-of-speech signal); median lead **258 ms**, max 3069 ms. Stricter: 10.5% began before the last word was spoken; the rest fired on the final words, in the pause before the end signal. False-trigger rate on the 15 turns that should not retrieve: 0%.
- G3 detail: 24/40 compound utterances isolated two or more sub-intents; full coverage of every gold intent 17.5%; over-fragmentation 1.7%; single-intent utterances kept single 100%.
  - by fixture family: {'compound': 60.0}
- G4 detail: support is pooled over every factual sentence the model wrote: 156 of 198 verified (a withheld sentence counts against it even though it never reached the user); per-turn mean 81.6%. 156 claims shipped across 99 answered turns; **0 fabricated citations shipped**, 1 blocked before shipping; recall@k (gold passages) 78.8% over 74 labelled turns; 108 turns flagged uncertainty.
- G4 independent audit: a second NLI model the engine never uses (`cross-encoder/nli-deberta-v3-base`) re-read the 156 shipped claims against the chunks they cite and agreed with 95.5% of them.
  - `asqa_compound_36` (support 0.464): Additional filming locations included Dyersburg and Union City, Tennessee, as well as Chester and Freeburg, Illinois .
  - `asqa_compound_38` (support 0.005): In 2009, Kobe Bryant played against the Orlando Magic in the NBA Finals, and the Lakers won the series 4 games to 1 .
  - `asqa_late_detail_09` (support 0.228): The theme tune for the British series Dear John was sung by Joan Baxter .
  - `asqa_late_detail_11` (support 0.151): Hank Aaron is the MLB all-time career RBI leader with 2,297 RBIs .
  - `asqa_late_detail_11` (support 0.151): Hank Aaron is the MLB all-time career RBI leader with 2,297 RBIs .
- G5 `asqa_late_detail_01`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_02`: v2 from v1, 2 claims preserved, 0 rewritten, full-corpus search `False`, 6 chunks carried from session state.
- G5 `asqa_late_detail_03`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_04`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_05`: v2 from v1, 1 claims preserved, 1 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_06`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 5 chunks carried from session state.
- G5 `asqa_late_detail_07`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 5 chunks carried from session state.
- G5 `asqa_late_detail_08`: v2 from v1, 2 claims preserved, 1 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_09`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_10`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 6 chunks carried from session state.
- G5 `asqa_late_detail_11`: v2 from v1, 2 claims preserved, 0 rewritten, full-corpus search `False`, 8 chunks carried from session state.
- G5 `asqa_late_detail_12`: v2 from v1, 0 claims preserved, 0 rewritten, full-corpus search `False`, 5 chunks carried from session state (the parent answer verified no claims, so none could be kept).
- G5 `asqa_late_detail_13`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `asqa_late_detail_14`: v2 from v1, 1 claims preserved, 0 rewritten, full-corpus search `False`, 4 chunks carried from session state.
- G5 `asqa_late_detail_15`: v2 from v1, 1 claims preserved, 1 rewritten, full-corpus search `False`, 3 chunks carried from session state.

## 3. Latency and cost

| Corpus | Turns | Retrieval lead (median) | TTFT (median) | TTFT p90 | Turn complete (median) | Cost/turn | Tokens/turn |
|---|---|---|---|---|---|---|---|
| enterprise | 22 | 259 ms | 2493 ms | 4228 ms | 3795 ms | $0.00223236 | 2143.68 |
| asqa | 120 | 258 ms | 3414 ms | 5720 ms | 3838 ms | $0.00228149 | 2257.51 |

Retrieval lead is measured from the trace: `utterance_end_ms - first_retrieval_ms`. 
TTFT is measured from the end of the utterance to the first *validated* token, because the engine 
releases a sentence only after its citations resolve and its support clears the bar.

## 4. Against a baseline pipeline

The baseline is a conventional RAG turn on the same fixtures, with the same retriever, reranker, synthesiser and verifier: nothing happens while the user speaks, then the whole utterance is one search (`SLR_CONTROLLER=batch`, `SLR_DECOMPOSE=off`). It has no notion of a presentation-only turn or of a late detail, so it searches on both. The difference between the rows is exactly what streaming, decomposition and session refinement add.

### enterprise corpus

| Pipeline | Retrieval before speech ends | Searches on reformat turns | Multi-intent (G3) | Late detail refined, not restarted (G5) | Citation support | Recall@k | Fabricated | TTFT after speech | Answer complete after speech | Cost/turn |
|---|---|---|---|---|---|---|---|---|---|---|
| streaming live RAG | 88.9% | 0% | 75% | 100% | 89.5% | 87.9% (documents) | 0 | 2493 ms | 3795 ms | $0.002232 |
| baseline (batch RAG) | 0% | 100% | 0% | 0% | 87.7% | 81.8% (documents) | 0 | 2290 ms | 3292 ms | $0.000307 |

### asqa corpus

| Pipeline | Retrieval before speech ends | Searches on reformat turns | Multi-intent (G3) | Late detail refined, not restarted (G5) | Citation support | Recall@k | Fabricated | TTFT after speech | Answer complete after speech | Cost/turn |
|---|---|---|---|---|---|---|---|---|---|---|
| streaming live RAG | 87.6% | 0% | 60% | 100% | 78.8% | 78.8% (passages) | 0 | 3414 ms | 3838 ms | $0.002281 |
| baseline (batch RAG) | 0% | 100% | 0% | 0% | 87.9% | 62.2% (passages) | 0 | 2260 ms | 2835 ms | $0.000264 |

## 5. Ablations

### retrieval branches

_Does the lexical branch earn its place, or would dense-only do?_

| Arm | Early retrieval | Multi-intent | Citation support | Recall@k | Intents with evidence | TTFT | Cost/turn |
|---|---|---|---|---|---|---|---|
| hybrid (bm25+dense) | 79.5% | 54.5% | 77.4% | 69.1% (passages) | 100% (0 starved) | 2856 ms | $0.003083 |
| dense only | 79.5% | 59.1% | 82.1% | 72.9% (passages) | 100% (0 starved) | 3966 ms | $0.003071 |


### per-intent quota

_What happens to the quiet sub-intent when the context window is filled by score alone?_

| Arm | Early retrieval | Multi-intent | Citation support | Recall@k | Intents with evidence | TTFT | Cost/turn |
|---|---|---|---|---|---|---|---|
| quota = 2 per intent | 79.5% | 50% | 78.2% | 66.8% (passages) | 100% (0 starved) | 2724 ms | $0.003035 |
| quota = 0 (global top-k) | 79.5% | 54.5% | 77.5% | 67.4% (passages) | 100% (0 starved) | 2760 ms | $0.003034 |


### retrieval controller

_Is a model call per transcript chunk worth what it costs?_

| Arm | Early retrieval | Multi-intent | Citation support | Recall@k | Intents with evidence | TTFT | Cost/turn |
|---|---|---|---|---|---|---|---|
| rule controller | 79.5% | 54.5% | 82.4% | 70% (passages) | 100% (0 starved) | 2686 ms | $0.003062 |
| model controller | 0% | 54.5% | 77.9% | 68.4% (passages) | 100% (0 starved) | 3288 ms | $0.003107 |


## 6. Analysed edge-case failures

127 failure instances across 5 categories, all taken from the run above.

### 1. claim withheld (39 instances)

**Example** — `compound_03` (enterprise corpus)

> Can you tell me how far in advance a venue has to be booked, and also which external caterers are approved for Baner Tech Hub?

**What happened.** 1 generated claim(s) failed verification and were withheld.

**Why.** The grounding verifier could not support a generated sentence from its cited chunk, so the sentence was withheld and reported as uncertainty instead of shipped. That is the intended behaviour, but each case is worth reading: a conservative verifier costs recall of true statements.

**Trace evidence.** `{'uncertainty': ['Not verified in the retrieved documents: Venues, including Baner Tech Hub, must be booked at least 10 working days before the event', 'The complete list of approved external caterers for Baner Tech Hub could not be verified from the retrieved documents.'], 'support_rate': 0.8, 'verifier': 'cross-encoder/nli-deberta-v3-xsmall'}`

Other instances: asqa_compound_03, asqa_compound_04, asqa_compound_08, asqa_compound_13, asqa_compound_14, asqa_compound_23, asqa_compound_24, asqa_compound_28

### 2. starved sub intent (38 instances)

**Example** — `late_detail_01` (enterprise corpus)

> Summarize the travel reimbursement rule for an employee trip.

**What happened.** A sub-intent reached the answer with no evidence of its own.

**Why.** A sub-intent reached synthesis with no evidence of its own. With the per-intent quota on this is usually a retrieval miss rather than a fusion one: the sub-query returned nothing above the margin cut.

**Trace evidence.** `{'sub_queries': ['employee travel reimbursement policy summary'], 'quota_applied': False}`

Other instances: asqa_compound_01, asqa_compound_09, asqa_compound_14, asqa_compound_17, asqa_compound_37, asqa_late_detail_01, asqa_late_detail_02, asqa_late_detail_05

### 3. under decomposition (33 instances)

**Example** — `compound_01` (enterprise corpus)

> I need to plan a customer workshop in Pune for 30 people, and I need the cancellation policy and the catering options.

**What happened.** 3 gold sub-intents, 2 sub-queries produced.

**Why.** Fewer sub-queries than gold sub-intents. Two different causes hide behind one symptom. On a request that bundles several asks it is a missed split: the clause splitter found no boundary, usually because the speaker used no punctuation and no conjunction that opens a new request. On an ASQA question it is not a parsing failure at all. 'Who has the highest goals in world football' has no second clause to find, and enumerating its readings (men's, women's, all-time) needs knowledge of the domain, which the deterministic splitter lacks and a language model has. This is where the model-backed decomposer earns its cost.

**Trace evidence.** `{'gold': ['Pune workshop venue with capacity for 30 people', 'venue cancellation and refund policy', 'catering options for a workshop in Pune'], 'predicted': ['customer workshop Pune 30 people cancellation policy', 'customer workshop Pune 30 people catering options'], 'merged_by_guard': 0, 'method': 'llm'}`

Other instances: asqa_compound_01, asqa_compound_02, asqa_compound_04, asqa_compound_05, asqa_compound_06, asqa_compound_07, asqa_compound_09, asqa_compound_10

### 4. late trigger (15 instances)

**Example** — `compound_04` (enterprise corpus)

> how much is the hotel limit for business trips in New York and what is the catering budget per attendee for a customer workshop

**What happened.** No provisional search fired before the speaker finished; retrieval began at the utterance end.

**Why.** The controller waits for content sufficiency and either semantic stability or a closed clause. A short utterance can end before any of those fire, so retrieval starts at the utterance end. This is the deliberate side of the trade: lowering the bar buys earlier retrieval and pays for it in searches on incomplete thoughts.

**Trace evidence.** `{'first_retrieval_ms': 11553, 'utterance_end_ms': 10082, 'decisions': [('wait', 'insufficient_content'), ('wait', 'insufficient_content'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('retrieve', 'utterance_complete')]}`

Other instances: asqa_compound_02, asqa_compound_06, asqa_compound_07, asqa_compound_11, asqa_compound_13, asqa_compound_21, asqa_compound_23, asqa_compound_26

### 5. over fragmentation (2 instances)

**Example** — `single_03` (enterprise corpus)

> What is the per diem rate and hotel limit when travelling to London for work?

**What happened.** 2 sub-queries for 1 gold sub-intents.

**Why.** More sub-queries than gold sub-intents — the failure the merge guard and the cap exist to bound. Extra near-duplicate queries dilute the reranker's budget and buy nothing.

**Trace evidence.** `{'gold': ['What is the per diem rate and hotel limit when travelling to London for work?'], 'predicted': ['current per diem rate business travel London', 'current hotel reimbursement limit business travel London']}`

Other instances: asqa_compound_08

## 7. Reproducing this

```bash
docker compose up --build      # or: make up
make eval                      # rebuilds any missing index, runs every fixture, rewrites this file
```

Fixtures live in `evals/fixtures/`, corpora in `evals/corpora/` and `data/corpus/` (ASQA, built by `make dataset`). 
`evals/results/latest.json` holds the full payload this report was rendered from, 
including every per-turn trace field the gates read.

