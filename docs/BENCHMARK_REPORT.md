# Benchmarking & Evaluation Report

**Streaming Live RAG — Samsung PRISM GenAI Hackathon, Theme 04**

Generated by `make eval` at 2026-09-18 15:14. Every number below is measured from that run; 
nothing is hand-entered. Re-running the command overwrites this file.

## 1. Run configuration

| | |
|---|---|
| embedder | `BAAI/bge-small-en-v1.5` |
| reranker | `cross-encoder/ms-marco-MiniLM-L-6-v2` |
| verifier | `cross-encoder/nli-deberta-v3-xsmall` |
| llm | `offline` |
| controller | `rule` |
| branches | `bm25,dense` |
| quota_per_intent | `2` |
| LLM configured | no — deterministic offline arms |
| replay speed | 1.0x |
| controller | `rule` |
| branches | `bm25,dense` |
| quota_per_intent | `2` |
| top_k | `8` |
| tau_stab | `0.95` |
| stab_run | `2` |
| merge_cos | `0.9` |
| support_min | `0.5` |
| llm | `auto` |
| reranker | `cross-encoder/ms-marco-MiniLM-L-6-v2` |

- **demo** corpus: 11 documents, 39 chunks

Total wall clock: 150.7s.

## 2. Acceptance gates

### demo corpus

| Gate | Criterion | Measured | Threshold | Result |
|---|---|---|---|---|
| G1 | Reproducibility | 100 | one command builds the index and runs the suite with no manual step | **PASS** |
| G2 | Early retrieval | 88.9% | >= 80% of eligible turns, with a low false-trigger rate | **PASS** |
| G3 | Multi-intent identification | 100% | >= 70% of compound utterances isolate >= 2 distinct sub-intents | **PASS** |
| G4 | Factual grounding | 100% | >= 85% citation support and zero fabricated citations | **PASS** |
| G5 | Session refinement | 100% | every late-detail turn: version 2, parent 1, claims preserved, no full-corpus search | **PASS** |
| G6 | Telemetry and observability | 100% | 100% of turns emit a complete trace record | **PASS** |

- G2 detail: 16/18 turns began retrieving before the utterance ended; median lead **260 ms**, max 6496 ms. False-trigger rate on presentation-only turns: 0%.
- G3 detail: 4/4 compound utterances isolated two or more sub-intents; full coverage of every gold intent 100%; over-fragmentation 0%; single-intent utterances kept single 100%.
  - by fixture family: {'compound': 100.0}
- G4 detail: 60 claims across 20 answered turns; **0 fabricated citations shipped**, 0 blocked before shipping; recall@k —% over 0 labelled turns; 5 turns flagged uncertainty.
- G5 `late_detail_01`: v2 from v1, 2 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.
- G5 `late_detail_02`: v2 from v1, 3 claims preserved, 0 rewritten, full-corpus search `False`, 4 chunks carried from session state.
- G5 `late_detail_03`: v2 from v1, 2 claims preserved, 0 rewritten, full-corpus search `False`, 3 chunks carried from session state.

## 3. Latency and cost

| Corpus | Turns | Retrieval lead (median) | TTFT (median) | TTFT p90 | Turn complete (median) | Cost/turn | Tokens/turn |
|---|---|---|---|---|---|---|---|
| demo | 22 | 260 ms | 469 ms | 1407 ms | 610 ms | $1.218e-05 | 0 |

Retrieval lead is measured from the trace: `utterance_end_ms - first_retrieval_ms`. 
TTFT is measured from the end of the utterance to the first *validated* token, because the engine 
releases a sentence only after its citations resolve and its support clears the bar.

## 5. Analysed edge-case failures

2 failure instances across 1 category, all taken from the run above.

### 1. late trigger (2 instances)

**Example** — `compound_04` (demo corpus)

> how much is the hotel limit for business trips in New York and what is the catering budget per attendee for a customer workshop

**What happened.** No provisional search fired before the speaker finished; retrieval began at the utterance end.

**Why.** The controller waits for content sufficiency and either semantic stability or a closed clause. A short utterance can end before any of those fire, so retrieval starts at the utterance end. This is the deliberate side of the trade: lowering the bar buys earlier retrieval and pays for it in searches on incomplete thoughts.

**Trace evidence.** `{'first_retrieval_ms': 10144, 'utterance_end_ms': 10088, 'decisions': [('wait', 'insufficient_content'), ('wait', 'insufficient_content'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('wait', 'intent_unstable'), ('retrieve', 'utterance_complete')]}`

Other instances: presentation_01

## 6. Reproducing this

```bash
docker compose up --build      # or: make up
make eval                      # rebuilds any missing index, runs every fixture, rewrites this file
```

Fixtures live in `evals/fixtures/`, corpora in `evals/corpora/` and `data/asqa/`. 
`evals/results/latest.json` holds the full payload this report was rendered from, 
including every per-turn trace field the gates read.

